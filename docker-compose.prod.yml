version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: acquisitions-app-prod
    env_file:
      - .env.production
    ports:
      - "3001:3001"
    restart: unless-stopped
